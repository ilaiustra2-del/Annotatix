<UserControl x:Class="HVAC.Module.UI.HVACPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Border Padding="20" Background="#F8F9FA">
        <StackPanel>
            <!-- Header -->
            <TextBlock Text="HVAC SuperScheme" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"
                       Foreground="#212529"
                       Margin="0,0,0,30"/>
            
            <!-- Description -->
            <TextBlock Text="Построение принципиальных схем вентиляции" 
                       FontSize="14" 
                       HorizontalAlignment="Center"
                       Foreground="#6C757D"
                       Margin="0,0,0,40"/>
            
            <!-- Commands Section -->
            <StackPanel Margin="20">
                <!-- Create Schema Button -->
                <Button x:Name="btnCreateSchema"
                        Content="🔧 Построение схемы"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Height="50"
                        Margin="0,0,0,15"
                        Background="#007BFF"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BtnCreateSchema_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#0056B3"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- Complete Schema Button -->
                <Button x:Name="btnCompleteSchema"
                        Content="✨ Достроение схемы"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Height="50"
                        Margin="0,0,0,15"
                        Background="#28A745"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BtnCompleteSchema_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1E7E34"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- Settings Button -->
                <Button x:Name="btnSettings"
                        Content="⚙️ Настройки"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Height="50"
                        Margin="0,0,0,15"
                        Background="#6C757D"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BtnSettings_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#545B62"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
            
            <!-- Info Section -->
            <Border Background="#E7F3FF" 
                    Padding="15" 
                    CornerRadius="5"
                    Margin="20,20,20,0">
                <StackPanel>
                    <TextBlock Text="ℹ️ Информация" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="#004085"
                               Margin="0,0,0,10"/>
                    <TextBlock TextWrapping="Wrap" 
                               FontSize="12" 
                               Foreground="#004085">
                        <Run Text="• "/>
                        <Run Text="Построение схемы: создаёт новую принципиальную схему на основе пространств"/>
                        <LineBreak/>
                        <Run Text="• "/>
                        <Run Text="Достроение схемы: добавляет новые элементы к существующей схеме"/>
                        <LineBreak/>
                        <Run Text="• "/>
                        <Run Text="Настройки: управление синхронизацией и параметрами модуля"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
