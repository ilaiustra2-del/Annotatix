<UserControl x:Class="HVAC.Module.UI.HVACPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White"
             FontFamily="Segoe UI">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Checkbox -->
        <CheckBox x:Name="chkSyncEnabled"
                  Grid.Row="0"
                  Content="Синхронизация чертежа с моделью"
                  FontSize="12"
                  Foreground="#333333"
                  Margin="0,0,0,16"
                  Checked="ChkSyncEnabled_CheckedChanged"
                  Unchecked="ChkSyncEnabled_CheckedChanged"/>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Button x:Name="btnBuildScheme"
                    Content="Построить схему"
                    Style="{StaticResource SecondaryButton}"
                    Click="BtnCreateSchema_Click"
                    Margin="0,0,12,0"/>
            <Button x:Name="btnRebuildScheme"
                    Content="Достроить схему"
                    Style="{StaticResource SecondaryButton}"
                    Click="BtnCompleteSchema_Click"/>
        </StackPanel>
        
        <!-- Spacer -->
        <Grid Grid.Row="2"/>
        
        <!-- Status Box -->
        <GroupBox Grid.Row="3"
                  Header="Статус"
                  MinHeight="60"
                  BorderBrush="#D0D0D0"
                  FontSize="12">
            <TextBlock x:Name="txtStatus"
                       Padding="8"
                       FontSize="12"
                       Foreground="#333333"
                       TextWrapping="Wrap"/>
        </GroupBox>
        
        <!-- Footer Buttons - REMOVED (not needed) -->
    </Grid>
</UserControl>
